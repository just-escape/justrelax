name: human_authenticator

default_publication_channel: d1.scenario
subscriptions:
  - d1.human_authenticator
  - d1.broadcast

serials:
  - port: /dev/arduino1
    baud_rate: 9600
    buffering_interval: 0.1
  - port: /dev/arduino2
    baud_rate: 9600
    buffering_interval: 0.1
