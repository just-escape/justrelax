host: localhost
port: 3031

name: human_authenticator
channel: digimiam1

node_params:
  authenticators:
    - port: /dev/arduino1
      baud_rate: 9600
      buffering_interval: 0.1
    - port: /dev/arduino2
      baud_rate: 9600
      buffering_interval: 0.1

logging: null
